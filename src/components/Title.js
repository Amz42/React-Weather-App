import React from 'react';

function Title(props) {
    return (
        <div>
            <h1 className="main-heading">Weather App</h1>
            <p>Find Weather of any State / City !!!</p>
        </div>
    );
}

export default Title;